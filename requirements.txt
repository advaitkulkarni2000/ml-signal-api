# ── Web Framework ─────────────────────────────────────────────
fastapi==0.111.0          # The web framework that serves our API
uvicorn==0.29.0           # ASGI server that runs FastAPI
pydantic==2.7.0           # Data validation for request/response schemas

# ── ML & Data ─────────────────────────────────────────────────
lightgbm==4.3.0           # Our gradient boosting model
scikit-learn==1.4.2       # Preprocessing, metrics
pandas==2.2.2             # DataFrame operations
numpy==1.26.4             # Numerical operations
yfinance==0.2.40          # Free market data (Yahoo Finance)
scipy==1.13.0             # Statistical computations

# ── MLflow — experiment tracking & model registry ─────────────
mlflow==2.13.0            # Tracks experiments, versions models

# ── Monitoring ────────────────────────────────────────────────
prometheus-client==0.20.0 # Exposes metrics for Prometheus to scrape

# ── Testing ───────────────────────────────────────────────────
pytest==8.2.0             # Test runner
httpx==0.27.0             # HTTP client for testing FastAPI endpoints

# ── Utilities ─────────────────────────────────────────────────
joblib==1.4.2             # Model serialisation (save/load)
python-dateutil==2.9.0
